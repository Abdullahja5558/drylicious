const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  // Ensure Tailwind CSS purge is enabled in tailwind.config.js for production builds to remove unused CSS and reduce render-blocking.
};

export default config;
